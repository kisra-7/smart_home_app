<resources>
    <!-- Enable QR scan in pairing flow -->
    <bool name="is_scan_support">true</bool>

    <!-- BLE support in pairing UI -->
    <bool name="is_need_ble_support">true</bool>
    <bool name="is_need_blemesh_support">true</bool>

    <!-- Hotspot ssid prefix -->
    <string name="ap_mode_ssid">SmartLife</string>

    <!-- ✅ Required when using auto-discovery with EZ support -->
    <bool name="is_config_activator_ez">true</bool>
</resources>
